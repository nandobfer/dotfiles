import React from "react"
import { Box } from "@mui/material"

interface ChatsProps {
    channel: string
}

export const Chats: React.FC<ChatsProps> = ({ channel }) => {
    return <Box sx={{}}>chats</Box>
}
